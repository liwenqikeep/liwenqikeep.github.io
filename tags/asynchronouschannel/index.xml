<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
	<channel>
		<title>AsynchronousChannel on Liwenqi&#39;s Blog</title>
		<link>https://liwenqikeep.github.io/tags/asynchronouschannel/</link>
		<description>Recent content in AsynchronousChannel on Liwenqi&#39;s Blog</description>
		<generator>Hugo</generator>
		<language>zh-cn</language>
		
		
		
			<copyright>Liwenqi</copyright>
		
		
			<lastBuildDate>Sat, 10 Feb 2018 00:00:00 +0000</lastBuildDate>
		
			<atom:link href="https://liwenqikeep.github.io/tags/asynchronouschannel/index.xml" rel="self" type="application/rss+xml" />
			<item>
				<title>AIO 异步 IO 与 CompletionHandler</title>
				<link>https://liwenqikeep.github.io/posts/aio-%E5%BC%82%E6%AD%A5-io-%E4%B8%8E-completionhandler/</link>
				<pubDate>Sat, 10 Feb 2018 00:00:00 +0000</pubDate>
				<guid>https://liwenqikeep.github.io/posts/aio-%E5%BC%82%E6%AD%A5-io-%E4%B8%8E-completionhandler/</guid>
				<description>&lt;h1 id=&#34;aio-异步-io-与-completionhandler&#34;&gt;AIO 异步 IO 与 CompletionHandler&lt;/h1&gt;&#xA;&lt;h2 id=&#34;aio-概述&#34;&gt;AIO 概述&lt;/h2&gt;&#xA;&lt;p&gt;AIO（Asynchronous IO）是 Java 7 引入的异步非阻塞 IO，告别轮询时代。&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code&gt;BIO:                          NIO:                        AIO:&#xA;[线程] ──阻塞读──→ [等待]     [线程] ←─select── [就绪?]    [线程] ──发起读──→ [完成?]&#xA;    ↑                          ↓                            ↓&#xA;返回数据                    返回 Channel               注册回调&#xA;    ↑                          ↓                            ↓&#xA;继续处理                    主动轮询                    OS 通知&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;h2 id=&#34;异步通道组&#34;&gt;异步通道组&lt;/h2&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-java&#34;&gt;// 创建异步通道组（线程池）&#xA;AsynchronousChannelGroup group = AsynchronousChannelGroup&#xA;    .withFixedThreadPool(4, Executors.defaultThreadFactory());&#xA;&#xA;// 或使用系统默认&#xA;AsynchronousChannelGroup group = &#xA;    AsynchronousChannelGroup.withCachedThreadPool(&#xA;        Executors.newCachedThreadPool(), &#xA;        10&#xA;    );&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;h2 id=&#34;asynchronousfilechannel&#34;&gt;AsynchronousFileChannel&lt;/h2&gt;&#xA;&lt;h3 id=&#34;创建&#34;&gt;创建&lt;/h3&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-java&#34;&gt;// 打开文件&#xA;AsynchronousFileChannel channel = AsynchronousFileChannel.open(&#xA;    Paths.get(&amp;quot;test.txt&amp;quot;),&#xA;    StandardOpenOption.READ,&#xA;    StandardOpenOption.WRITE&#xA;);&#xA;&#xA;// 使用通道组&#xA;AsynchronousFileChannel channel = AsynchronousFileChannel.open(&#xA;    Paths.get(&amp;quot;test.txt&amp;quot;),&#xA;    Set.of(StandardOpenOption.READ),&#xA;    group&#xA;);&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;h3 id=&#34;读取&#34;&gt;读取&lt;/h3&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-java&#34;&gt;// 方式1：使用 Future&#xA;Future&amp;lt;Integer&amp;gt; future = channel.read(&#xA;    ByteBuffer.allocate(1024),  // 缓冲区&#xA;    0                           // 文件位置&#xA;);&#xA;&#xA;// 阻塞等待结果&#xA;Integer bytesRead = future.get();&#xA;System.out.println(&amp;quot;读取了 &amp;quot; + bytesRead + &amp;quot; 字节&amp;quot;);&#xA;&#xA;// 方式2：使用 CompletionHandler 回调&#xA;channel.read(&#xA;    ByteBuffer.allocate(1024),&#xA;    0,&#xA;    null,&#xA;    new CompletionHandler&amp;lt;Integer, ByteBuffer&amp;gt;() {&#xA;        @Override&#xA;        public void completed(Integer result, ByteBuffer buffer) {&#xA;            System.out.println(&amp;quot;读取了 &amp;quot; + result + &amp;quot; 字节&amp;quot;);&#xA;            buffer.flip();&#xA;            // 处理数据&#xA;        }&#xA;        &#xA;        @Override&#xA;        public void failed(Throwable exc, ByteBuffer buffer) {&#xA;            exc.printStackTrace();&#xA;        }&#xA;    }&#xA;);&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;h3 id=&#34;写入&#34;&gt;写入&lt;/h3&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-java&#34;&gt;// Future 方式&#xA;ByteBuffer buffer = ByteBuffer.allocate(1024);&#xA;buffer.put(&amp;quot;Hello, AIO!&amp;quot;.getBytes());&#xA;buffer.flip();&#xA;&#xA;Future&amp;lt;Integer&amp;gt; future = channel.write(buffer, 0);&#xA;Integer bytesWritten = future.get();&#xA;&#xA;// CompletionHandler 方式&#xA;channel.write(&#xA;    buffer,&#xA;    0,&#xA;    buffer,&#xA;    new CompletionHandler&amp;lt;Integer, ByteBuffer&amp;gt;() {&#xA;        @Override&#xA;        public void completed(Integer result, ByteBuffer buffer) {&#xA;            System.out.println(&amp;quot;写入了 &amp;quot; + result + &amp;quot; 字节&amp;quot;);&#xA;        }&#xA;        &#xA;        @Override&#xA;        public void failed(Throwable exc, ByteBuffer buffer) {&#xA;            exc.printStackTrace();&#xA;        }&#xA;    }&#xA;);&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;h2 id=&#34;asynchronousserversocketchannel&#34;&gt;AsynchronousServerSocketChannel&lt;/h2&gt;&#xA;&lt;h3 id=&#34;服务器示例&#34;&gt;服务器示例&lt;/h3&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-java&#34;&gt;public class AIOHttpServer {&#xA;    private static final int PORT = 8080;&#xA;    &#xA;    public static void main(String[] args) throws Exception {&#xA;        AsynchronousServerSocketChannel server = &#xA;            AsynchronousServerSocketChannel.open();&#xA;        &#xA;        server.bind(new InetSocketAddress(PORT));&#xA;        System.out.println(&amp;quot;AIO Server started on port &amp;quot; + PORT);&#xA;        &#xA;        // 接受连接&#xA;        server.accept(null, new CompletionHandler&amp;lt;AsynchronousSocketChannel, Void&amp;gt;() {&#xA;            @Override&#xA;            public void completed(AsynchronousSocketChannel client, Void attachment) {&#xA;                // 继续接受下一个连接&#xA;                server.accept(null, this);&#xA;                &#xA;                // 处理客户端请求&#xA;                handleClient(client);&#xA;            }&#xA;            &#xA;            @Override&#xA;            public void failed(Throwable exc, Void attachment) {&#xA;                exc.printStackTrace();&#xA;            }&#xA;        });&#xA;        &#xA;        // 防止主线程退出&#xA;        Thread.sleep(Long.MAX_VALUE);&#xA;    }&#xA;    &#xA;    private static void handleClient(AsynchronousSocketChannel client) {&#xA;        ByteBuffer buffer = ByteBuffer.allocate(1024);&#xA;        &#xA;        client.read(buffer, buffer, new CompletionHandler&amp;lt;Integer, ByteBuffer&amp;gt;() {&#xA;            @Override&#xA;            public void completed(Integer result, ByteBuffer buffer) {&#xA;                if (result == -1) {&#xA;                    try {&#xA;                        client.close();&#xA;                    } catch (IOException e) {&#xA;                        e.printStackTrace();&#xA;                    }&#xA;                    return;&#xA;                }&#xA;                &#xA;                buffer.flip();&#xA;                String request = StandardCharsets.UTF_8.decode(buffer).toString();&#xA;                System.out.println(&amp;quot;Request: &amp;quot; + request);&#xA;                &#xA;                // 响应&#xA;                String response = &amp;quot;HTTP/1.1 200 OK\r\n&amp;quot; +&#xA;                    &amp;quot;Content-Type: text/plain\r\n&amp;quot; +&#xA;                    &amp;quot;Content-Length: 2\r\n&amp;quot; +&#xA;                    &amp;quot;\r\n&amp;quot; +&#xA;                    &amp;quot;OK&amp;quot;;&#xA;                &#xA;                ByteBuffer responseBuffer = &#xA;                    ByteBuffer.wrap(response.getBytes());&#xA;                &#xA;                client.write(responseBuffer, responseBuffer, &#xA;                    new CompletionHandler&amp;lt;Integer, ByteBuffer&amp;gt;() {&#xA;                        @Override&#xA;                        public void completed(Integer result, ByteBuffer buffer) {&#xA;                            try {&#xA;                                client.close();&#xA;                            } catch (IOException e) {&#xA;                                e.printStackTrace();&#xA;                            }&#xA;                        }&#xA;                        &#xA;                        @Override&#xA;                        public void failed(Throwable exc, ByteBuffer buffer) {&#xA;                            exc.printStackTrace();&#xA;                        }&#xA;                    }&#xA;                );&#xA;            }&#xA;            &#xA;            @Override&#xA;            public void failed(Throwable exc, ByteBuffer buffer) {&#xA;                exc.printStackTrace();&#xA;            }&#xA;        });&#xA;    }&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;h2 id=&#34;completionhandler-接口&#34;&gt;CompletionHandler 接口&lt;/h2&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-java&#34;&gt;public interface CompletionHandler&amp;lt;V, A&amp;gt; {&#xA;    // 操作成功完成时调用&#xA;    void completed(V result, A attachment);&#xA;    &#xA;    // 操作失败时调用&#xA;    void failed(Throwable exc, A attachment);&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;h3 id=&#34;参数说明&#34;&gt;参数说明&lt;/h3&gt;&#xA;&lt;table&gt;&#xA;&#x9;&lt;thead&gt;&#xA;&#x9;&#x9;&#x9;&lt;tr&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;th&gt;参数&lt;/th&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;th&gt;说明&lt;/th&gt;&#xA;&#x9;&#x9;&#x9;&lt;/tr&gt;&#xA;&#x9;&lt;/thead&gt;&#xA;&#x9;&lt;tbody&gt;&#xA;&#x9;&#x9;&#x9;&lt;tr&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;td&gt;V result&lt;/td&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;td&gt;操作结果（读取字节数、写入字节数等）&lt;/td&gt;&#xA;&#x9;&#x9;&#x9;&lt;/tr&gt;&#xA;&#x9;&#x9;&#x9;&lt;tr&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;td&gt;A attachment&lt;/td&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;td&gt;附加对象，用于在回调中传递上下文&lt;/td&gt;&#xA;&#x9;&#x9;&#x9;&lt;/tr&gt;&#xA;&#x9;&lt;/tbody&gt;&#xA;&lt;/table&gt;&#xA;&lt;h3 id=&#34;使用场景&#34;&gt;使用场景&lt;/h3&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-java&#34;&gt;// 使用 attachment 传递上下文&#xA;public class HttpHandler {&#xA;    public void handle(AsynchronousSocketChannel client) {&#xA;        ByteBuffer readBuffer = ByteBuffer.allocate(1024);&#xA;        ByteBuffer writeBuffer = ByteBuffer.allocate(1024);&#xA;        &#xA;        // 创建上下文&#xA;        HttpContext context = new HttpContext(client, readBuffer, writeBuffer);&#xA;        &#xA;        client.read(readBuffer, context, new CompletionHandler&amp;lt;&amp;gt;() {&#xA;            @Override&#xA;            public void completed(Integer result, HttpContext ctx) {&#xA;                // 可以访问上下文中的所有数据&#xA;                ctx.getClient();&#xA;                ctx.getReadBuffer();&#xA;                ctx.getWriteBuffer();&#xA;            }&#xA;        });&#xA;    }&#xA;}&#xA;&#xA;class HttpContext {&#xA;    private final AsynchronousSocketChannel client;&#xA;    private final ByteBuffer readBuffer;&#xA;    private final ByteBuffer writeBuffer;&#xA;    &#xA;    public HttpContext(AsynchronousSocketChannel client, &#xA;                      ByteBuffer readBuffer, ByteBuffer writeBuffer) {&#xA;        this.client = client;&#xA;        this.readBuffer = readBuffer;&#xA;        this.writeBuffer = writeBuffer;&#xA;    }&#xA;    &#xA;    // getters...&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;h2 id=&#34;future-vs-completionhandler&#34;&gt;Future vs CompletionHandler&lt;/h2&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-java&#34;&gt;// Future 方式：同步等待结果&#xA;Future&amp;lt;Integer&amp;gt; future = channel.read(buffer, 0);&#xA;while (!future.isDone()) {&#xA;    // 可以做其他事情&#xA;}&#xA;Integer result = future.get();  // 阻塞获取结果&#xA;&#xA;// CompletionHandler 方式：回调通知&#xA;channel.read(buffer, 0, null, new CompletionHandler&amp;lt;&amp;gt;() {&#xA;    @Override&#xA;    public void completed(Integer result, ByteBuffer buffer) {&#xA;        // 自动回调，结果在这里处理&#xA;    }&#xA;});&#xA;// 继续执行其他代码，不阻塞&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;h2 id=&#34;超时处理&#34;&gt;超时处理&lt;/h2&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-java&#34;&gt;// 设置超时&#xA;AsynchronousSocketChannel channel = AsynchronousSocketChannel.open();&#xA;&#xA;// 使用 timeout 参数&#xA;channel.read(&#xA;    buffer,&#xA;    5000,  // 5秒超时&#xA;    TimeUnit.MILLISECONDS,&#xA;    null,&#xA;    new CompletionHandler&amp;lt;&amp;gt;() {&#xA;        @Override&#xA;        public void completed(Integer result, Void attachment) {&#xA;            // 读取成功&#xA;        }&#xA;        &#xA;        @Override&#xA;        public void failed(Throwable exc, Void attachment) {&#xA;            if (exc instanceof InterruptedByTimeoutException) {&#xA;                System.out.println(&amp;quot;读取超时&amp;quot;);&#xA;            }&#xA;        }&#xA;    }&#xA;);&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;h2 id=&#34;完整示例异步文件复制&#34;&gt;完整示例：异步文件复制&lt;/h2&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-java&#34;&gt;public class AsyncFileCopy {&#xA;    &#xA;    public Future&amp;lt;Long&amp;gt; copy(String src, String dst) {&#xA;        try {&#xA;            AsynchronousFileChannel srcChannel = AsynchronousFileChannel.open(&#xA;                Paths.get(src), StandardOpenOption.READ);&#xA;            AsynchronousFileChannel dstChannel = AsynchronousFileChannel.open(&#xA;                Paths.get(dst), &#xA;                StandardOpenOption.CREATE, &#xA;                StandardOpenOption.WRITE);&#xA;            &#xA;            final long[] totalBytes = {0};&#xA;            long size = srcChannel.size();&#xA;            &#xA;            return copyRecursive(srcChannel, dstChannel, 0, size);&#xA;            &#xA;        } catch (IOException e) {&#xA;            throw new RuntimeException(e);&#xA;        }&#xA;    }&#xA;    &#xA;    private Future&amp;lt;Long&amp;gt; copyRecursive(&#xA;            AsynchronousFileChannel src, &#xA;            AsynchronousFileChannel dst,&#xA;            long position,&#xA;            long size) {&#xA;        &#xA;        if (position &amp;gt;= size) {&#xA;            return CompletableFuture.completedFuture(size);&#xA;        }&#xA;        &#xA;        ByteBuffer buffer = ByteBuffer.allocateDirect(8192);&#xA;        &#xA;        return src.read(buffer, position).thenCompose(bytesRead -&amp;gt; {&#xA;            if (bytesRead == -1) {&#xA;                return CompletableFuture.completedFuture(position);&#xA;            }&#xA;            &#xA;            buffer.flip();&#xA;            return dst.write(buffer, position).thenCompose(bytesWritten -&amp;gt; {&#xA;                return copyRecursive(src, dst, position + bytesWritten, size);&#xA;            });&#xA;        });&#xA;    }&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;h2 id=&#34;与-nio-对比&#34;&gt;与 NIO 对比&lt;/h2&gt;&#xA;&lt;table&gt;&#xA;&#x9;&lt;thead&gt;&#xA;&#x9;&#x9;&#x9;&lt;tr&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;th&gt;特性&lt;/th&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;th&gt;NIO&lt;/th&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;th&gt;AIO&lt;/th&gt;&#xA;&#x9;&#x9;&#x9;&lt;/tr&gt;&#xA;&#x9;&lt;/thead&gt;&#xA;&#x9;&lt;tbody&gt;&#xA;&#x9;&#x9;&#x9;&lt;tr&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;td&gt;编程模型&lt;/td&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;td&gt;Reactor（主动轮询）&lt;/td&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;td&gt;Proactor（被动通知）&lt;/td&gt;&#xA;&#x9;&#x9;&#x9;&lt;/tr&gt;&#xA;&#x9;&#x9;&#x9;&lt;tr&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;td&gt;线程模型&lt;/td&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;td&gt;单线程 + Selector&lt;/td&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;td&gt;线程池 + 回调&lt;/td&gt;&#xA;&#x9;&#x9;&#x9;&lt;/tr&gt;&#xA;&#x9;&#x9;&#x9;&lt;tr&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;td&gt;适用场景&lt;/td&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;td&gt;中等并发&lt;/td&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;td&gt;高并发&lt;/td&gt;&#xA;&#x9;&#x9;&#x9;&lt;/tr&gt;&#xA;&#x9;&#x9;&#x9;&lt;tr&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;td&gt;API 复杂度&lt;/td&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;td&gt;较低&lt;/td&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;td&gt;较高&lt;/td&gt;&#xA;&#x9;&#x9;&#x9;&lt;/tr&gt;&#xA;&#x9;&#x9;&#x9;&lt;tr&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;td&gt;性能&lt;/td&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;td&gt;好&lt;/td&gt;&#xA;&#x9;&#x9;&#x9;&#x9;&#x9;&lt;td&gt;更好&lt;/td&gt;&#xA;&#x9;&#x9;&#x9;&lt;/tr&gt;&#xA;&#x9;&lt;/tbody&gt;&#xA;&lt;/table&gt;&#xA;&lt;h2 id=&#34;总结&#34;&gt;总结&lt;/h2&gt;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;AIO 是真正的异步 IO，无需轮询&lt;/li&gt;&#xA;&lt;li&gt;AsynchronousFileChannel 处理异步文件操作&lt;/li&gt;&#xA;&lt;li&gt;AsynchronousServerSocketChannel/AsynchronousSocketChannel 处理异步网络&lt;/li&gt;&#xA;&lt;li&gt;CompletionHandler 替代 Future 做回调&lt;/li&gt;&#xA;&lt;li&gt;适合高并发、长连接场景&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;h2 id=&#34;系列总结&#34;&gt;系列总结&lt;/h2&gt;&#xA;&lt;p&gt;本系列文章涵盖了 Java IO 的核心内容：&lt;/p&gt;</description>
			</item>
	</channel>
</rss>
